# 项目级 Agent 规则

本文件适用于整个仓库。每次在新窗口中处理本项目时，先阅读 `README.md` 和 `PROJECT_CONTEXT.md`，再运行 `git status --short` 了解当前工作区。

## 博客发布流程

这是一个部署到 GitHub Pages 的 Hugo 博客。写博客或修改博客内容时，必须遵循以下顺序：

1. 先在 `content/posts/` 创建或修改本地文章。
2. 新文章保持可发布状态：front matter 使用 `draft = false`，或不设置 `draft`。
3. 使用下面的命令启动本地预览：

   ```bash
   hugo server --bind 127.0.0.1 --port 1313 --disableFastRender
   ```

4. 把对应的 `http://localhost:1313/` 预览地址交给用户检查。
5. 用户未明确表示“审查通过”“发布”或“推送”前，不得提交或推送博客文章。
6. 用户明确审查通过并要求发布时，执行 Hugo 构建检查，只暂存本次任务涉及的文件，然后提交并推送到 `origin/main`。

线上是否发布以 Git 推送为边界，而不是依赖草稿状态：文章可以在本地保持 `draft = false`，只要没有推送，线上博客就不会变化。不要把“写一篇博客”“修改文章”“生成预览”理解为允许推送；推送必须来自用户单独、明确的指令。

## 博客写作规范

当用户只提供一段话、零散素材或一个主题并要求写博客时：

- 保持简要、明确、具体，减少套话、重复和无信息量的过渡句。
- 不为了显得完整而扩写用户没有提供、也无法可靠确认的事实。
- 默认按照“原因 → 架构/方案 → 总结”的路线组织正文。
- 第一部分先说明为什么要做、遇到了什么问题或需求从何而来。
- 第二部分讲清整体架构、关键组成、数据或操作流程，以及必要的实现细节。
- 最后一部分总结结果、价值和仍需注意的事项，不重复全文。
- 技术文章优先使用清楚的标题和短段落；只有在能提高可读性时才使用列表。
- 保留作者的第一人称视角和个人经验，但避免夸张、自我宣传和空泛感想。

推荐的默认结构如下，可按题材删减，但不要打乱主线：

```markdown
## 为什么要做这件事

交代背景、问题和目标。

## 整体架构与实现

说明组成部分、数据流、关键步骤和取舍。

## 总结

概括结果、价值和注意事项。
```

## Git 与文件安全

- 修改前和提交前都检查 `git status --short`。
- 不使用 `git add .`；只暂存用户本次明确要求涉及的文件。
- 不修改、删除或提交无关的未跟踪草稿。
- 不直接编辑 `public/` 中的生成页面；修改 `content/`、`data/`、`layouts/` 或 `static/` 中的源文件。
- 推送前运行 `git diff --check` 和一次 Hugo 构建检查。
